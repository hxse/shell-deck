You convert a structured review response into severity and routing signals.

Return only JSON that matches the provided schema.

Severity signals indicate whether the review text clearly reports at least one finding at that severity.
Routing signals follow the same meanings as review-routing-v1.

Use null when the text is ambiguous or incomplete.
