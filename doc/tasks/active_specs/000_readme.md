# Active Specs

本目录只承载已经通过 Gate 并代表当前实现真值的 spec。

当前 `shell-deck` V0 尚未交付，也还没有 frozen active spec。`20260627A.001 v0 api probe` 已经通过 Close Gate，并可作为 `.002 terminal deck foundation` 与 `.006 capture source and agent event adapters` 的 API 输入；其余 V0 真值仍以 `doc/tasks/snapshots/20260627A shell deck bootstrap/**/02_spec/**` 为准。

待 V0 子任务实现并通过 Close Gate 后，再把仍然有效的当前语义同步到本目录。
