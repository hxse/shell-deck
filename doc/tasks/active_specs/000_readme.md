# Active Specs

本目录记录已经落地的current-schema implementation truth。当前真值对应`.032`的multi-Room/user-level storage、`.033`的single-controller content edit lease、`.034`的Macro editor/Prepare/runner、`.035`的server-authoritative Room runtime sync与统一mutation feedback、`.037`的MacroDefinitionV4 assigned/unassigned reference、`.038`的MacroDefinitionV5 explicit AgentEvent wait limit、`.039`的retained terminal tab views、`20260722B`的Tailwind/daisyUI与browser-local Theme system、`20260722C`的`business`默认Theme、`20260723A`的native daisyUI control surface contract、`20260723B`的behavior-preserving coordinator ownership、`20260723C`的bounded mechanical ownership与默认400行Gate、`20260724A`的Library domain hard removal、`20260724B`的structured JSON submission Capture、`20260724C`的Macro editor input stability、`20260724D`的full-stack hot-path incremental transport与pagination、`20260726A`的显式Guest/Authenticated与Local/LAN browser admission、`20260729A`的MacroDefinitionV6 flexible Parallel shared Text routing，以及`20260729B`的Parallel默认fail与terminal deck controls。MacroRecord是唯一saved user-content model。

当前文件：

* `shell_deck_architecture.md`
* `room_terminal_contract.md`
* `user_data_storage_contract.md`
* `macro_template_contract.md`
* `run_log_contract.md`
* `capture_agent_event_contract.md`
* `parser_profile_contract.md`
* `ui_theme_contract.md`
* `access_control_contract.md`

旧 config、Project、Directory scope、TerminalDeck、terminal alias、旧 Macro V2 route 与外部 AgentEvent fallback 不再是 active truth。历史设计与各阶段 Gate 记录保留在 `doc/tasks/snapshots/20260627A shell deck bootstrap/**`。
