# Active Specs

本目录记录已经落地的 current-schema implementation truth。当前 foundation 真值对应 `20260627A.032 multi-room routing and user-level content storage`；该 change 不是 standalone release，Room controller/content lease、Macro V3 integration 与 Library 分别由后继 `.033`、`.034`、`.035` 接续。

当前文件：

* `shell_deck_architecture.md`
* `room_terminal_contract.md`
* `user_data_storage_contract.md`
* `macro_template_contract.md`
* `run_log_contract.md`
* `capture_agent_event_contract.md`
* `parser_profile_contract.md`

旧 config、Project、Directory scope、TerminalDeck、terminal alias、旧 Macro V2 route 与外部 AgentEvent fallback 不再是 active truth。历史设计与各阶段 Gate 记录保留在 `doc/tasks/snapshots/20260627A shell deck bootstrap/**`。
