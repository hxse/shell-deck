# 20260627A.010 Macro GUI Smoke Test Matrix

* 父任务：`20260627A shell deck bootstrap`
* 状态：draft-doc
* 类型：test-hardening
* 目标：用真实浏览器 GUI 操作验证 macro 功能能在普通 shell 和普通 shell 内的 Codex 中跑通。
* 非目标：不新增 macro 能力，不重写 runner，不把 API-only 测试当作本任务的主验收。
