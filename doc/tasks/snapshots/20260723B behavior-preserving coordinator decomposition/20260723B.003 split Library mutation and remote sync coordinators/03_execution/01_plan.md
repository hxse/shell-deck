# Execution Plan

## 阶段一：baseline与ports

* 冻结factory return surface、rune variables、navigation identity及published Save branches。
* 运行Library unit/HTTP与`.036` CRUD/navigation/races/reconnect baseline。
* 定义Library-specific mutation outcome与remote live snapshot/commit ports。

## 阶段二：MutationWorkflow

* 移动Library client proxy、fresh edit/remove lease与best-effort release。
* 移动create/update/fresh-created-lease transaction，保持navigation/current guards。
* factory保留Save/Remove/Cancel public command和唯一state commit/reconcile adapter。

## 阶段三：RemoteSyncCoordinator

* 移动invalidation queue、serialized drain、read generation、connection/focus refresh和retry timer。
* effects只转发connection/content events；mount/dispose只装配focus/search timer与lease release。
* factory通过live snapshot与commit callbacks继续唯一更新list/item/status/notice/error。

## 阶段四：closeout

* 增加single-rune-owner、sole-assembly、no-generic-session与public-boundary断言。
* 运行static/build/full unit/integration和focused browser Gate。
* 同步active Library contract、review/index，确认`jj conflict=false`后进入`.004`。
