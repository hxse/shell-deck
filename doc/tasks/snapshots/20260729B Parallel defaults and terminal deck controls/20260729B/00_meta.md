# 20260729B Parallel defaults and terminal deck controls

## 任务概括

把新建Parallel的pane failure默认值改为`fail`，让Parallel pane使用与递归Flow一致的theme-native depth缩进线；同时把`Prepare terminals`缩短为`Prepare`，并在同一run dock增加带确认的`Close all`，一次关闭当前Room的全部Shell和Text。

## 正式级别

二星任务。默认值和presentation是局部变更，但Close all跨越browser确认、controller guard、terminal structure serialization、backend close与authoritative index projection，存在批量结构mutation quietly wrong风险。

## 范围

范围内：新建Parallel默认值；Parallel pane层级rail；Prepare显示标签；Close all UI/protocol/server批量路径；focused regression；current docs与guide同步。

范围外：修改已保存Macro的显式`onLaneFail`；修改Parallel failure runtime；自动Prepare；关闭Room；删除Macro/run/evidence；terminal close rollback；重做run dock布局或既有单terminal close。
