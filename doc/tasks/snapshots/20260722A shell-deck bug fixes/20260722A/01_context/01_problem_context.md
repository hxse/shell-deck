# Problem Context

`20260721A`只做行为等价拆分，因此即使拆分期间或后续使用中发现真实产品问题，也不能混入该重构链。当前需要一个新的ownership入口，把bug的复现证据、contract变化、实现和回归测试放在同一条可审阅历史中。

本系列不是无限范围的cleanup bucket。每个子任务必须说明用户可观察的问题、最小修复边界以及明确不变的语义；无法证明归属的“顺手优化”留在后续任务。
