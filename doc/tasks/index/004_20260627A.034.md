# Task Index 20260627A.034

## Task ID `20260627A.034`

### Summary

* 路径：doc/tasks/snapshots/20260627A shell deck bootstrap/20260627A.034 macro terminal layout prepare validation and run snapshot/
* 状态：implementation-gate-passed（Macro V3、显式Prepare、version-bound Start与memory-only runner已落地；Library consumer由.035承接）
* 范围：把production Macro CRUD/editor/runner原子接入.032 MacroRecord/resource transaction与.033 controller/edit lease；MacroDefinitionV3只保存typed index/type和Flow body；唯一validator返回stable issues；terminalId跟随对象、index跟随UI，Start按调用者看到的revision一次解析为immutable run snapshot与manifest attachment。
* 相关active spec / guide：本task 02_spec/**已按实现收口；Macro、terminal、runner、architecture、README与quickstart已同步，Library业务UI仍由.035承接。
* 摘要：Save只做portable definition validation，不依赖Room terminal；dirty Start把Save返回revision/lease release视为明确phase transition，再按原terminal revision启动。Prepare只由Macro面板`Prepare terminals`按钮显式触发，消费当前visual/JSON draft的validated terminalLayout snapshot，不要求clean或已保存；backend失败立即报错、保留已成功调整并让用户修复。event log仍是run lifecycle truth，manifest只是run_started引用的immutable attachment。

### Relations

* 父任务：20260627A。
* 硬前置：.032的Room/Home lifecycle、User Data Root、MacroRecord/resource transaction与.033的single-writer controller/content edit lease。
* 修正：.003/.005的TerminalTarget/action-time live index，以及旧Start resolver/reactive repair/Destroy lock设计。
* 延续：.021 shell/text capability、.025 live runtime cursor和.031 editor dirty/pending/revision lock。
* 前置于：.035 Library macro-template content必须使用MacroDefinitionV3；clean Load创建并切换、dirty Load只创建，两者都不Prepare。
* 排除：Room/storage ownership、Library CRUD、notification config、AgentEvent ingest、PTY restoration、migration和cross-process live Room sync。
