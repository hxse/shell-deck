# 20260627A Task Index

## Root Task `20260627A`

### Summary
* 路径：`doc/tasks/snapshots/20260627A shell deck bootstrap/20260627A/`
* 状态：v0-closeout
* 范围：创建 `shell-deck` 项目入口与 V0 蓝图，明确产品方向、交付边界、阶段拆分和 legacy kill list。
* 相关 active spec / guide：active specs 已同步到 `doc/tasks/active_specs/**`；snapshot group 保留设计和审阅历史。
* 摘要：`shell-deck` 从 browser terminal 底座出发，保留 terminal/PTY/multi-tab sync/input latency stabilization；parser 主输入改为用户可选 capture source，默认 terminal-buffer，AgentEvent/Codex hook 可选；通过 shell-deck `justfile` 的 `codex` recipe 临时注入 Codex hook，不修改用户全局 Codex 配置；hook adapter 统一上报 `AgentEvent Ingest`；明确不迁移 role / sealed instruction / Codex orchestration。

### Relations
* 父任务：无。
* 子任务：`20260627A.001` 到 `20260627A.026`。
* 外部参考：只把 `orch-web` 作为 terminal 底座经验来源，不把它的 role / Codex orchestration 当作新项目方向。

## Subtasks

### `20260627A.001 v0 api probe`
* 路径：`doc/tasks/snapshots/20260627A shell deck bootstrap/20260627A.001 v0 api probe/`
* 状态：close-gate-passed
* 目标：验证 V0 底层 API：shell-deck `justfile` 的 `codex` recipe、临时 hook config 注入、SessionStart/Stop payload、AgentEvent Ingest、terminal deck WebSocket baseline、multi-tab sync、config isolation、PTY input/resize/replay 和 terminal-buffer capture。
* V0 贡献：已通过 `just test-001-offline` 和 `just test-001-online`，作为 `.002` terminal API 与 `.006` Codex hook adapter 的已验证输入。

### `20260627A.002 terminal deck foundation`
* 路径：`doc/tasks/snapshots/20260627A shell deck bootstrap/20260627A.002 terminal deck foundation/`
* 状态：implementation-landed；automated gate passed
* 目标：初始化可运行项目，交付 local server、多 terminal slot、真实 PTY、output replay、multi-tab sync 和输入延迟基线。
* V0 贡献：用户能在浏览器里可靠使用多个编号 terminal，这是 macro panel 的执行底座。

### `20260627A.003 macro template workbench`
* 路径：`doc/tasks/snapshots/20260627A shell deck bootstrap/20260627A.003 macro template workbench/`
* 状态：implementation-landed；automated gate passed；manual smoke pending
* 目标：交付 macro template schema、持久化、基础可视化 CRUD、复制、导入、导出、terminal ref、alias、capture source/profile 引用配置，以及 `.003` 自带的最小 `ProfileCatalogSummary` stub。
* V0 贡献：用户能不用手写 JSON 管理可复用宏模板，并通过备份文件迁移模板。

### `20260627A.004 run log and artifacts`
* 路径：`doc/tasks/snapshots/20260627A shell deck bootstrap/20260627A.004 run log and artifacts/`
* 状态：implementation-landed；automated gate passed
* 目标：交付 append-only run event log、artifact 存储、derived state rebuild 和节点日志视图。
* V0 贡献：先建立宏运行唯一真值和可观测底座，后续 runner/parser 都写入这同一份日志。

### `20260627A.005 macro runner state machine`
* 路径：`doc/tasks/snapshots/20260627A shell deck bootstrap/20260627A.005 macro runner state machine/`
* 状态：implementation-landed；automated gate passed
* 目标：交付 `send_line`、`wait`、`sleep`、`input_line`、`capture-source`、`parse` stub、`branch`、`goto`、带 `loopGuard.maxIterations` 的简单回跳循环、`pause/complete/fail/resume/stop` 的最小 runner 状态机，先接 mock capture/parser stub。完整 `for_each`、item binding、嵌套循环、`break` 和 `continue` 留给 V1。
* V0 贡献：宏能真的执行、暂停、恢复，并且每一步都可追溯。

### `20260627A.006 capture source and agent event adapters`
* 路径：`doc/tasks/snapshots/20260627A shell deck bootstrap/20260627A.006 capture source and agent event adapters/`
* 状态：implementation-landed；offline gate passed；online optional/blocked by environment
* 目标：交付默认 terminal-buffer capture、AgentEvent ingest endpoint、Codex Stop hook adapter、JSONL spool fallback 和 capture artifact 写入。
* V0 贡献：runner 能从用户选择的 source 获得真实 capture artifact，Codex session id 可追溯但不绑定 session。

### `20260627A.007 parser profiles and adapters`
* 路径：`doc/tasks/snapshots/20260627A shell deck bootstrap/20260627A.007 parser profiles and adapters/`
* 状态：implementation-landed；offline gate passed；online optional/blocked by environment
* 目标：交付 parser adapter interface、`regex` parser、`ai-json`/codex exec one-shot probe、与 `.003` summary stub 兼容的内置 ParserProfile bundle、schema validation、normalize、soft signals、replicas disagreement pause 和 fixture eval。
* V0 贡献：宏能用内置 parser profile 把 capture artifact 解析成结构化分支信号，同时保持 parser 只是 soft guard。

### `20260627A.008 bounded parallel lanes`
* 路径：`doc/tasks/snapshots/20260627A shell deck bootstrap/20260627A.008 bounded parallel lanes/`
* 状态：implementation-landed；offline gate passed
* 目标：交付单个 macro run 内的受限 `parallel_all` fan-out/fan-in，lane 复用通用 capture/parser/condition 能力，并禁止同 config 多 live run。
* V0 贡献：支持并发审阅多个不同 terminal，同时保持一个 run、一份 event log、无 terminal 输入交错。

### `20260627A.009 v0 closeout`
* 路径：`doc/tasks/snapshots/20260627A shell deck bootstrap/20260627A.009 v0 closeout/`
* 状态：implementation-landed；automated closeout passed；human smoke pending
* 目标：交付 quickstart、active specs 同步、端到端验收、review/verification 报告和已知限制。
* V0 贡献：把前八个任务收束成可交付 V0，确保文档、测试和实际行为一致。

### `20260627A.010 macro gui smoke test matrix`
* 路径：`doc/tasks/snapshots/20260627A shell deck bootstrap/20260627A.010 macro gui smoke test matrix/`
* 状态：draft-doc
* 目标：用真实浏览器 GUI 操作验证 macro 功能能在普通 shell 和普通 shell 内的 Codex 中跑通；普通 shell 为离线必过，Codex-in-shell 为 online smoke，可因 auth/network/model quota blocked。
* V0 贡献：补上用户真实操作路径的宏功能验收，避免 API-only/e2e 层面通过但 GUI 上不可用。

### `20260627A.011 flow control redesign`
* 路径：`doc/tasks/snapshots/20260627A shell deck bootstrap/20260627A.011 flow control redesign/`
* 状态：draft-doc
* 目标：重设计 macro flow control 的 GUI 和 schema，把普通动作与流程控制分栏显示，使用接近 Python 的 `if/elif/else/for/break/continue/finish` block-tree 心智模型，并把 `pause/stop` 收回到 runner 控制按钮。
* V0 关系：这是 post-V0 / Flow V2 设计任务，不改变 `.009/.010` 已定义的当前 V0 交付面；后续若实现，将作为宏编辑器和 runner 的下一轮 UX/schema 演进。

### `20260627A.012 Flow V2 language cleanup and lane unification`
* 路径：`doc/tasks/snapshots/20260627A shell deck bootstrap/20260627A.012 Flow V2 language cleanup and lane unification/`
* 状态：implementation-landed；automated gate passed
* 目标：在进入 Flow V2 compiler/interpreter 和后续 macro UI 重排前，冻结“全面清理旧语法”的语言边界：Flow V2 主语言禁止 `next/branch/goto/loopGuard` 等 v1 控制字段；`parallel_all` 收窄为 send/wait/capture 的 fan-out/fan-in 原语，并通过 `merge_parallel_results` 与 `send_artifact` 处理聚合结果。
* V0 关系：这是 post-V0 / Flow V2 清理任务，不改变当前 V0 交付面；它是后续 Flow V2 compiler/interpreter 和 `.013` workspace side panels UI 设计的前置语言边界。

### `20260627A.013 workspace side panels and prompt library`
* 路径：`doc/tasks/snapshots/20260627A shell deck bootstrap/20260627A.013 workspace side panels and prompt library/`
* 状态：implementation-landed；automated gate passed
* 目标：重设计 workspace side panels，让 Macro / Prompt 可显示隐藏、可拖拽宽度、可按 config 持久化，并交付支持增删查改、搜索、浏览、复制的 project/global Prompt Library。
* V0 关系：这是 post-V0 / workspace UX 设计任务，不改变当前 V0 交付面；它为后续更长宏模板和 prompt 复用提供主界面基础设施。

### `20260627A.014 macro prompt workbench UI cleanup`
* 路径：`doc/tasks/snapshots/20260627A shell deck bootstrap/20260627A.014 macro prompt workbench UI cleanup/`
* 状态：implementation-landed；automated gate passed
* 目标：清理 Macro / Prompt / Run Log 的信息架构，把模板和 prompt 选择改成 searchable selector，把 Run Log / AI Trace 收进 Macro 顶层 Trace tab，让 Run Log 实时更新，并把 terminal tab close 作为 workbench chrome cleanup 明确纳入。
* V0 关系：这是 post-V0 / workspace UX cleanup 任务，不改变 runner/template/prompt 存储语义；它修复 .013 交付后的可用性问题。

### `20260627A.015 macro workbench component boundary stabilization`
* 路径：`doc/tasks/snapshots/20260627A shell deck bootstrap/20260627A.015 macro workbench component boundary stabilization/`
* 状态：implementation-landed；automated gate passed
* 目标：在不改变功能、逻辑语义和 `.014` workbench 视觉布局的前提下，拆分 Macro/workspace workbench 的组件、样式和文件边界；重点处理超过 400 行且直接影响 UI 边界的 `MacroPanel.svelte`、`App.svelte` 和 `src/styles.css`，把 workbench chrome 与后续 Flow V2 action/message-flow 改造隔离开。
* V0 关系：这是 post-V0 / architecture boundary refactor 任务，不改变当前 V0 交付面；它是 `.016` macro action message flow redesign 的前置任务。

### `20260627A.016 macro action message flow redesign`
* 路径：`doc/tasks/snapshots/20260627A shell deck bootstrap/20260627A.016 macro action message flow redesign/`
* 状态：implementation-landed；automated gate passed
* 目标：重新收敛 Flow V2 的 action 与 message flow：新建模板删除 `sleep`、`parse` 和内置 `ai-json` action；文本获取统一走 `capture-source`，文本发送统一走 `send_line`/`input_line` 的 message parts，文本判断直接进入 `if.text_match` condition；并发能力从 `parallel_all`/`merge_parallel_results` 收窄为 `parallel_send_capture`，只封装多个普通 send/capture 并机械合并结果；旧 Flow UI 替换为 Py-like `if`/`elif`/`else`/`for`/`break`/`continue`/`finish`。
* V0 关系：这是 post-V0 / Flow V2 功能实现任务；已在 `.015` 组件边界稳定后落地，且未修改 `.014` workbench chrome。


### `20260627A.017 macro node insertion interaction redesign`
* 路径：`doc/tasks/snapshots/20260627A shell deck bootstrap/20260627A.017 macro node insertion interaction redesign/`
* 状态：implementation-landed；automated gate passed
* 目标：重新设计 Macro Flow V2 node editor 的插入交互：每个节点提供 Add before / Add after / Remove / Move up / Move down，控制流节点提供 Add inside if/elif/else/for/control；动作选择改为 floating insertion palette，删除常驻右侧 Actions/Flow 栏目标设计，并用 Py-like 竖线/缩进强化嵌套结构。同时补齐 `for` 的 count/forever 两种模式、`finish` / `break` / `continue` 的 action-only body、`extract_text.onEmpty=continue`，以及 Terminal target 的 index/alias/id 合并显示。
* V0 关系：这是 post-V0 / macro editor UX design task；主线是插入交互和视觉层级，同时补齐 `.016` 暴露出的 `for` count/forever 和 control terminal action-only body 两个控制流缺口。

### `20260627A.018 parallel lane tab output redesign`
* 路径：`doc/tasks/snapshots/20260627A shell deck bootstrap/20260627A.018 parallel lane tab output redesign/`
* 状态：implementation-landed；automated gate passed
* 目标：把 `parallel_send_capture` 收敛为 `parallel` lane tab 模型：每个 lane 是可视化 tab，lane 内只允许 `send_line`、`wait`、`capture-source`、`extract_text` 四类顺序 action，并以必选、最终、不可删除的固定 `Output` 节点声明该 lane 合并结果；parent `parallel` 统一产出 `merged_text`。
* V0 关系：这是 post-V0 / macro language and editor UX task；主线是降低并发宏的 schema 和可视化复杂度，同时保留受限 fan-out/fan-in 能力。

### `20260627A.019 codex hook-only capture mode`
* 路径：`doc/tasks/snapshots/20260627A shell deck bootstrap/20260627A.019 codex hook-only capture mode/`
* 状态：implementation-landed；automated gate passed
* 目标：把 Codex AgentEvent capture 收敛到 hook 直接支持的字段：`UserPromptSubmit.prompt`、`Stop.last_assistant_message` 和 `SessionStart` 元数据；新增 required `captureMode = result_only | prompt_only | prompt_and_result`，删除旧 `eventKind` / `field` macro 语法，并修复 `just -f ... codex` target cwd 与 hook data root 分离。
* V0 关系：这是 post-V0 / Codex hook adapter contract fix；不读取 transcript、TUI、`codex exec --json`、reasoning、tool calls 或中间步骤。

### `20260627A.020 tab naming and macro target source labels`
* 路径：`doc/tasks/snapshots/20260627A shell deck bootstrap/20260627A.020 tab naming and macro target source labels/`
* 状态：implementation-landed；automated gate passed
* 目标：把默认 tab alias 从泛化 `terminal_N` 收敛为 backend-aware 的 `shell_N` / `text_N`，并把 macro selector 的用户可见文案统一为 `Target tab` / `Source tab` / `Lane tab`；底层 JSON `terminal` 字段保持不变。
* V0 关系：这是 post-V0 / naming-contract polish；不改变 runner、schema、Codex hook 或 session 语义。

### `20260627A.021 tab capability aware macro controls`
* 路径：`doc/tasks/snapshots/20260627A shell deck bootstrap/20260627A.021 tab capability aware macro controls/`
* 状态：implementation-landed；automated gate passed
* 目标：让 Macro Flow V2 编辑器根据所选 deck tab 的 backend capability 收窄可见控件和可选 action，避免 text tab 暴露 `terminal-quiet`、`terminal-buffer`、`agent-event` 等 shell-only 能力。
* V0 关系：这是 post-V0 / macro editor capability implementation；不修改 macro JSON 字段名，不改变 runner 已支持的 send/input/text append 行为。

### `20260627A.022 explicit macro send enter semantics`
* 路径：`doc/tasks/snapshots/20260627A shell deck bootstrap/20260627A.022 explicit macro send enter semantics/`
* 状态：implementation-landed；automated gate passed
* 目标：把 Macro Flow V2 的发送动作从隐式 `send_line` / `input_line` 收敛为显式 `send` / `input`，用 `enter: boolean` 表达是否提交，并把 macro runner 的提交符统一为 LF。
* V0 关系：这是 post-V0 / macro language contract fix；不做旧模板兼容迁移，不自动 trim 用户文本，不把补充提交符写回 `message`。

### `20260627A.023 home cwd and path prompt for real shell`
* 路径：`doc/tasks/snapshots/20260627A shell deck bootstrap/20260627A.023 home cwd and path prompt for real shell/`
* 状态：implementation-landed；automated gate passed
* 目标：让真实 shell tab 默认进入用户 HOME，显示加粗亮绿色的 user@host:cwd prompt 并在 `$` 后保留分隔空格，提供颜色 alias，同时优先使用本地 Maple Mono 等宽字体 fallback。
* V0 关系：这是 post-V0 / terminal UX polish；不改变 terminal protocol、macro runner、Codex hook 或 text/fake backend 行为。

### `20260627A.024 macro notification action`
* 路径：`doc/tasks/snapshots/20260627A shell deck bootstrap/20260627A.024 macro notification action/`
* 状态：implementation-landed；automated gate passed
* 目标：新增 Flow V2 `notify` action，支持 shell-deck 内置浮窗/声音、浏览器系统通知和 Telegram Bot 频道通知；Telegram 凭据只放本机 ignored 配置，仓库只跟随 example。
* V0 关系：这是 post-V0 / macro action extension；不改变 send/input/wait/capture/parallel 语义，也不把第三方通知凭据写入 macro template。

## V0 Completion Rule

九个功能子任务完成并通过 `.009` closeout gate 后，`shell-deck` V0 的功能面可以视为收束：local-only config-scoped web terminal deck 可用，`codex` just recipe hook adapter 与统一 AgentEvent ingest 可用，macro template 可按 index、id 或 alias 引用 terminal，macro run 可追溯可恢复，可执行 send-line、sleep、input-line、带 `loopGuard.maxIterations` 的简单回跳循环、受限 `parallel_all` 和基础流程控制，capture source 可选 terminal-buffer 或 AgentEvent，parser 可选 `regex` 或 `ai-json`，Codex session id 可追溯但不绑定 session。

注意：上段是 `.009` closeout 时代的历史 V0 描述；当前 macro language truth 以 `.016` / `.017` / `.018` 和 `doc/tasks/active_specs/macro_template_contract.md` 为准。

`.010` 是 V0 用户路径验收加固任务：在正式宣称宏功能对用户可用前，需要通过普通 shell GUI smoke；Codex-in-shell GUI smoke 若受外部环境阻塞，必须记录 blocked reason。

`.011` 和 `.012` 是 V0 之后的 Flow V2 设计/清理任务：它们记录新的流程控制方向和语言边界，但不作为当前 V0 completion blocker。

`.013` 是 V0 之后的 workspace side panel 与 Prompt Library 设计任务：它记录主界面布局和 prompt CRUD 方向，也不作为当前 V0 completion blocker。

`.014` 是 V0 之后的 Macro / Prompt / Run Log workbench UI cleanup 任务：它修复 .013 后暴露的信息架构和实时追溯问题，不作为当前 V0 completion blocker。

`.015` 是 V0 之后的 Macro workbench component boundary stabilization 任务：它只规划组件/文件拆分，不改变功能、schema、runner 或 layout 语义；它是后续 Flow V2 功能改造的前置边界任务。

`.016` 是 V0 之后的 macro action/message-flow redesign 任务：它是旧 `.015` 功能规划顺延后的任务，依赖 `.015` 完成后实现，并且不得改动 `.014` workbench chrome；当前 macro language truth 以 `.016` / `.017` 与 active spec 为准。

`.017` 是 V0 之后的 Macro node insertion interaction redesign 任务：它已落地节点局部插入、floating insertion palette 和 Py-like 嵌套视觉；并补齐 `for` count/forever、`finish` / `break` / `continue` action-only body、`extract_text.onEmpty=continue` 和 Terminal target 合并显示。除这些缺口外，不改变 `.016` action/message/capture/extract 语义。

`.018` 是 V0 之后的 parallel lane tab output redesign 任务：它把并发宏收敛为受限 lane tab 与固定 Output 节点，当前并发能力以 `.018` 和 active spec 为准。

`.019` 是 V0 之后的 Codex hook-only capture mode 修复任务：它把 AgentEvent capture 收敛到 Codex hook 直接提供的 prompt/result 字段，当前 Codex hook capture truth 以 `.019` 和 `doc/tasks/active_specs/capture_agent_event_contract.md` 为准。

`.020` 是 V0 之后的 tab naming polish 任务：它固定 `shell_N` / `text_N` 默认 alias 和 Macro selector 的 `Target tab` / `Source tab` / `Lane tab` 文案；内部 JSON 字段仍以 active spec 的 `terminal` ref 为准。

`.021` 是 V0 之后的 tab capability aware Macro controls 落地任务：它增加 live tab capability helper，让 Macro editor 和 runner start preflight 根据 shell/text tab 能力过滤或拒绝 wait/capture/parallel lane 配置；内部 JSON 字段仍保持 `terminal` ref。

`.022` 是 V0 之后的 macro send/input 语义修复任务：它把发送内容和提交动作拆成 `message` / runtime input 与 `enter`，当前设计真值已同步到 active spec。

`.023` 是 V0 之后的 real shell terminal UX polish 任务：它让真实 shell 默认进入用户 HOME，给 bash 设置加粗亮绿色的 user@host:cwd 默认 prompt 并保留 `$` 后分隔空格，提供颜色 alias，同时让浏览器终端优先使用本地 Maple Mono 字体 fallback 和 normal 字重；当前 terminal deck truth 已同步到 active spec。

`.024` 是 V0 之后的 macro notification action 任务：它新增 `notify` action，并把 Telegram bot token / channel id 限定在本机 `.shell-deck/notification-profiles.json`，仓库只跟随 example 配置。

## Task ID `20260627A.025`

### Summary

* 路径：`doc/tasks/snapshots/20260627A shell deck bootstrap/20260627A.025 macro execution cursor and text-list templates/`
* 状态：implementation-landed；automated close gate passed
* 范围：为 Flow V2 for 新增 text-list iteration 与 scoped `{{text}}` templates，并用 occurrence-aware execution cursor 修正 loop/if/parallel/wait/input 的正常 pause/resume 和 artifact resolution。
* 相关 active spec / guide：本项是历史交付；text-list/template 当前真值已由 `.026` 破坏性替换，cursor/capture 保留部分分别见当前 active specs。
* 摘要：普通 text 保持 literal，用户通过 per-field/part checkbox 显式开启 template；lexical descendants 继承最近 text-list binding。Runner 使用可序列化 occurrence cursor 与安全 operation boundary，从准确 invocation 恢复并避免重复已完成 side effect。

### Relations

* 父任务：`20260627A`。
* 修正：`20260627A.005` 的最小 static-step pause/resume model 在 Flow V2 loop occurrence 下不足。
* 扩展：`20260627A.017` 的 for/editor、`20260627A.018` 的 parallel lanes、`20260627A.022` 的 send/input content semantics，以及 `20260627A.024` 的 notify content surface。
* 保持：`20260627A.019` 的 hook-only capture 与 `20260627A.021` 的 tab capability contract 不变。

## Task ID 20260627A.026

### Summary

* 路径：doc/tasks/snapshots/20260627A shell deck bootstrap/20260627A.026 structured text-list bindings and adaptive text editors/
* 状态：implementation-landed；automated close gate passed
* 范围：把 text-list 破坏性替换为 structured key/value items 和 {{index}}/{{key}}/{{value}} binding，并统一 Macro multiline 正文的 adaptive height 与临时手动 resize。
* 相关 truth：当前真值已同步到 `doc/tasks/active_specs/macro_template_contract.md`、`doc/tasks/active_specs/run_log_contract.md` 与 `doc/guides/001_quickstart.md`；.025 snapshot 保持历史不变。
* 摘要：旧 string[]、{{text}}、entry-list、alias/migration/Convert 全部拒绝；index 是纯 1-based 数字且不持久化。Adaptive textarea 按实际 scrollHeight 加一行自动增长至 per-surface cap，允许不持久化的 temporary manual height，hard cap 60vh。

### Relations

* 父任务：20260627A。
* 破坏性替换：20260627A.025 的 text-list items:string[] 和唯一 {{text}} grammar。
* 保持：20260627A.025 的 occurrence-aware cursor、artifact scope、pause/resume safe boundary 和 S1-S6 capability whitelist。
* 扩展：Macro message、prompt、description、runtime input 与 text-list Value 的共享 multiline editor。
