# Task Index 20260725C

## Task ID `20260725C`

### Summary

* 路径：`doc/tasks/snapshots/20260725C persisted log storage quota/20260725C/`
* 状态：Formal Document、Code、Test、Current Docs与Close Gate完成；范围内未解决P1/P2/P3为零。
* 级别：三星任务。
* 范围：为run evidence/artifacts与AgentEvent raw logs增加默认2 GiB shared quota、精确90% GC水位、whole-run/closed-segment oldest-first retention、跨process current-run pin与scan-to-publish atomic admission；Trace index known mutation/cold rebuild同样使用quota → trace锁顺序，AgentEvent切换为逐line quota检查的8 MiB numbered rolling segment。
* 破坏性边界：旧`<generation>.jsonl` flat AgentEvent layout不迁移、不dual-read，以`legacy_agent_event_log_unsupported`失败。
* 相关active spec：已同步`run_log_contract.md`、`capture_agent_event_contract.md`、`user_data_storage_contract.md`与`shell_deck_architecture.md`；Quickstart新增quota配置、删除顺序和受保护数据说明。
* 审阅：`04_review/01_review_and_verification.md`记录外部审阅1个P1、2个P2、2个P3的修复、failure/concurrency oracle、残余风险与完整串行Gate。

### Relations

* `follows 20260725B`：落实其明确留给后续正式任务的retention policy，不重开server consistency change。
* `follow-up 20260724D`：沿用bounded run event tail与Trace cold freshness truth，并为completed run/artifact总量增加跨process GC。
* `follow-up 20260624A.032/.038`：AgentEventStore继续唯一拥有index/waiter，storage layout current-schema替换为可安全回收的open/closed segment。
* Origin/session proof、HTTP/WS/Macro resource envelope、AgentEvent memory eviction及age/count/pin/export仍不属于本change。
