# Task Index 20260627A.038

## Task ID `20260627A.038`

### Summary

* 路径：`doc/tasks/snapshots/20260627A shell deck bootstrap/20260627A.038 explicit agent-event capture wait limit/`
* 状态：implementation-complete；Document/Code/Test Gate通过。
* 范围：把AgentEvent Capture的隐藏10分钟全局超时替换为MacroDefinitionV5中的exact `waitLimit`；默认无限等待，用户可在visual/JSON editor显式启用duration timeout。
* 运行时：无限等待只由匹配output、Stop、Room/launch失效、server restart或hook error终止；显式timeout按active waiting time计算并fail loudly。

### Relations

* 父任务：20260627A。
* 前置：.032 AgentEvent ingest/store、.034 runner、.037 MacroDefinitionV4 exact references。
* 替换：V4与`SHELL_DECK_AGENT_EVENT_CAPTURE_TIMEOUT_MS`隐藏行为。
* 后继：20260721A纯拆分任务链只保留本任务行为，不得重新设计。
