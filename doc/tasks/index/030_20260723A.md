# Task Index 20260723A

## Task ID `20260723A`

### Summary

* 路径：`doc/tasks/snapshots/20260723A native DaisyUI control surfaces/20260723A/`
* 状态：Formal Document、Code、Test与Document Gate完成；未解决P1/P2为零。
* 范围：保留`business`默认Theme，撤销应用自定义dark palette token，把button、text input、select与textarea收口到daisyUI原生component/variant；enabled command使用solid semantic action hierarchy，普通field使用统一theme-derived fill，saved Macro view保持full-contrast content且normal read-only notice可直接进入既有Edit lease流程，并只为真实结构边界保留theme-derived divider。
* 相关 active spec / guide：`ui_theme_contract.md`、active spec入口与Quickstart已同步。
* 摘要：应用级dark palette override与Macro ancestor control emulation清零；interactive element直接使用daisyUI component，command使用solid semantic/tertiary ghost层级，普通field统一为ghost + `bg-base-content/15`；saved Macro由`alert-info alert-soft`标记read-only而不压暗内容，提示本身支持click/Enter/Space并委托canonical `beginEdit()`，flow node以primary/secondary/accent/info纵向guide和同色渐隐横向separator表达depth；默认`btn`、`btn-soft` command及相邻`base-*` field fill清零。默认style Gate按Svelte semantics、Macro presentation、app.css/theme ownership和shared helper拆分，原入口保持facade/CLI兼容。

### Relations

* `follow-up 20260722C`：保留其browser default、first-paint和valid preference语义；替代其dark `base-300`/`depth` override与“所有control显式消费强边界”的视觉策略。
* `refines 20260722B.003/.004`：不恢复legacy stylesheet；继续使用唯一framework入口和utility/component class，同时去除Macro ancestor control emulation。
