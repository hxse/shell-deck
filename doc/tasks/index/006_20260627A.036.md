# Task Index 20260627A.036

## Task ID `20260627A.036`

### Summary

* 路径：doc/tasks/snapshots/20260627A shell deck bootstrap/20260627A.036 user content library and macro template instantiation/
* 状态：implementation-complete，Document/Code/Test Gate通过。
* 范围：在.032 User Data Root/canonical resource transaction、.033 controller/content edit lease与.034 MacroDefinitionV3/validator/CRUD基线上新增单一user-global Library；canonical identity为(kind,itemId)，三类content支持CRUD、稳定search、clipboard Copy、Refresh和Macro Load。
* 相关active spec / guide：`active_specs/library_contract.md`、`macro_template_contract.md`、`user_data_storage_contract.md`、`shell_deck_architecture.md`与quickstart已同步。
* 摘要：saved Library item默认read-only，Edit/Save/Delete消费owner-server-renewed lease与revision，并和lease transition共用单一resource transaction guard；Copy只复制当前content且clipboard失败不伪报成功。Macro JSON保持invalid_json/invalid_macro_definition分层并逐字消费.034 validator result，Load每次创建fresh MacroRecord；clean editor创建并切换，dirty/locked editor只创建不切换。

### Relations

* 父任务：20260627A。
* 硬前置：.032 User Data Root/shared paths/store/generated ID，.033 controller/content edit lease，以及.034 MacroRecord CRUD/MacroDefinitionV3/validator/显式`Prepare terminals` contract。
* 替换：.013的Prompt-only store/panel能力；旧executable contract已由.032 hard cut。
* 延续：.014 side-panel workbench交互与.031/.034 editor dirty/pending/revision lock。
* 保持：.027 current-schema-only和旧语法fail-loudly原则。
* 排除：Room/generated-ID重设计、terminal resolver实现、scope、notification config、migration、cloud/cross-OS sync和automatic run。
