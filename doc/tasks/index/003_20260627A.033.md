# Task Index 20260627A.033

## Task ID `20260627A.033`

### Summary

* 路径：doc/tasks/snapshots/20260627A shell deck bootstrap/20260627A.033 single-writer room control and shared content edit leases/
* 状态：implementation-gate-passed（代码已落地；Macro/Library业务consumer由.034/.036承接）
* 范围：为每个live Room建立server-enforced single controller，并为user-global Macro/Library saved record建立跨Room/process的per-record content edit lease；包含owner-only capability、server-owned heartbeat、takeover、TTL、与.032 resource transaction/revision组合及observer UI。
* 相关 active spec / guide：本task 02_spec/**已按实现收口；multi-client、storage、architecture、README与quickstart已同步，Macro/Library业务consumer边界保留给.034/.036。
* 摘要：同一用户多设备连接同一Room时只有一个writer，其他连接只收到无secret的observer view并可操作纯local UI；owner server按WebSocket liveness续租，不依赖background browser timer。不同Room/process编辑同一长期record时也只有一个editor，lease transition与record commit共用.032 canonical resource transaction。Room Home lifecycle Destroy由.032管理，不伪造target controller。

### Relations

* 父任务：20260627A。
* 前置：.032的Room/client identity、Room Home/lifecycle边界、User Data Root、canonical resource transaction与revision primitive。
* 回归基线：继续演进`.031B` current journey；新增Take Control结构归因与六个surviving control的controller-only行为归因，任何无single-writer依据的Room/terminal差异仍须回到所属前序task修代码。
* 前置于：.034的Macro editor/Prepare/Start/run snapshot与Macro lease接入；.036的Library editor/Load与Library lease接入。
* 排除：多人协作模型、CRDT/OT、cross-process live Room federation、MacroDefinitionV3、Library内容schema和持久化controller。
