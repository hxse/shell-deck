# Task Index 20260627A.039

## Task ID `20260627A.039`

### Summary

* 路径：`doc/tasks/snapshots/20260627A shell deck bootstrap/20260627A.039 retained terminal tab views/`
* 状态：implementation-complete；Document/Code/Test Gate通过。
* 范围：修复Shell/Text tab切换销毁并重建terminal view、反复解析完整replay的问题；访问过的view在terminal存活期间保留，切换只改变可见性；同时收口current full E2E discovery与`.031A`历史journey隔离。
* 性能边界：未访问terminal仍延迟到首次选择才hydration；tab切换不得重新写历史，terminal删除、Room退出或明确的server replace仍按既有生命周期清理或重建。

### Relations

* 父任务：20260627A。
* 演进：修正`.032`的browser terminal view lifecycle，同时保留`.030`的大replay有界解析和`.032`的server-authoritative replay/revision contract。
* 实现基线：直接承接`20260627A.038`，并作为后续`20260721A`纯拆分任务的输入；后续拆分不得改变本任务冻结的terminal view生命周期。
