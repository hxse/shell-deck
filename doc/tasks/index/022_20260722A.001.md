# Task Index 20260722A.001

## Task ID `20260722A.001`

### Summary

* 路径：`doc/tasks/snapshots/20260722A shell-deck bug fixes/20260722A.001 Macro workbench authoring and run feedback/`
* 状态：implementation、自审、active spec同步与Close Gate完成。
* 范围：修复Macro workbench的For item插入、active run只读状态与current stage反馈、App通知重复次数/间隔、Parallel无text output authoring，以及loop template token controls。

### Relations

* 父任务：`20260722A`。
* 基线：直接承接`20260722A`根change与`20260721A.010`产品状态。
* schema hard cut：App notify channel新增required `repeatCount/repeatIntervalMs`；缺字段或越界值直接validation fail，不补默认、不迁移。
* 保持：Parallel runtime/schema、server frozen run snapshot、Room controller/content lease、显式Prepare及其它Macro布局/行为。

### Close Gate

* `just check`、`just build`、`just test-unit`、`just test-001`、`just test-e2e`与`just diff-check`全部通过。
* 最终计数：178 unit、53 integration、38 focused unit、7 focused browser、55 full E2E。
