# Task Index 20260722B

## Task ID `20260722B`

### Summary

* 路径：`doc/tasks/snapshots/20260722B themeable UI system migration/20260722B/`
* 状态：根任务Document Gate完成；implementation按`.001`至`.004`四个独立子任务和顺序jj change推进。
* 范围：在保持current UI结构、产品行为与runtime contract不变的前提下，全面移除迁移前project-authored CSS，迁移到Tailwind CSS 4与daisyUI 5，并提供`system`及daisyUI全部35个内置theme。
* 唯一允许的UI结构变化：在Room现有Settings popover中增加一个Theme选择控件；不新增顶栏Theme按钮，不给Home另建Settings，不改变其他DOM层级、控件位置、panel布局或responsive结构。

### Relations

* 基线：已完成的`20260722A.002`。
* 子任务：`.001`建立framework/theme foundation；`.002`迁移App、Room与terminal chrome；`.003`迁移Macro与Library workbench；`.004`删除legacy CSS并完成全theme closeout。
* 保持：Room/controller、terminal、Macro、Library、runner、storage record与测试journey的产品语义。
* 排除：页面重排、控件增删或搬家、navigation重做、schema兼容层、无关功能开发，以及借换肤重写交互。
