# Task Index 20260726A

## Task ID `20260726A`

### Summary

* 路径：`doc/tasks/snapshots/20260726A access modes and browser authentication/20260726A/`
* 状态：Formal Document、Code、Test、Current Docs与Close Gate完成；范围内未解决P1/P2/P3为零。
* 级别：三星任务。
* 范围：增加Guest/Authenticated访问模式与Local/LAN监听模式、ephemeral token换browser session、HTTP/WS同源保护，以及八个Just原生launch recipe alias。
* 相关active spec / guide：已同步`access_control_contract.md`、architecture、Quickstart与README。
* 审阅：`04_review/01_review_and_verification.md`记录Host/Origin统一admission、Vite page/source gate、session、LAN/dev/hook边界、400行新增量与完整串行Gate。
* 摘要：鉴权只作为进入server的门锁，不增加账号/角色；Room ID不承载secret。LAN正式支持同Wi-Fi手机访问，server不检测client IP。启动使用`sgl/sgn/sal/san`与`dgl/dgn/dal/dan`，不由shell解析mode；NixOS临时firewall通过显式`fw-open/show/reset`管理，不隐式提权。

### Relations

* `follow-up 20260725B`：承接其明确留给独立正式任务的browser security边界。
* `refines 20260627A.032`：把旧local-only无认证启动破坏性替换为显式access/listen组合。
* `related 20260721A.001`：在既有HTTP/WebSocket route facade之前增加统一admission，不重做route domain。
* `follows 20260725C`：作为当前jj栈顶后继，不改变log quota与retention contract。
