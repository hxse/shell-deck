# Task Index 20260727A

## Task ID `20260727A`

### Summary

* 路径：`doc/tasks/snapshots/20260727A QR token login/20260727A/`
* 状态：Formal Document、Execution、Code、Focused Test与Current Docs Gate完成；范围内未解决P1/P2/P3为零。完整E2E受既有large PTY load-sensitive用例阻断，待正常主机负载下取得绿色Close Gate证据。
* 级别：二星任务。
* 范围：Authenticated CLI输出raw-token terminal QR；login page使用现有DaisyUI `business` theme UI，通过独立图片上传或Secure Context实时camera在browser本地解析并沿用既有login POST。
* 相关active spec / guide：已同步`access_control_contract.md`、Quickstart与README。
* 摘要：使用单一零依赖`qr` package生成与解析；实时loop限制720-pixel frame、单任务、最快8 FPS并按工作耗时降频。Plain HTTP LAN保留图片/手工fallback，不引入HTTPS、图片上传、QR URL或第二token schema。

### Relations

* `follow-up 20260726A`：在既有browser authentication门锁上增加token的QR输入路径，不改变session、Origin或Room admission。
