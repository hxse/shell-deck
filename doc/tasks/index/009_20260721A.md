# Task Index 20260721A

## Task ID `20260721A`

### Summary

* 路径：`doc/tasks/snapshots/20260721A behavior-preserving large-file decomposition/20260721A/`
* 状态：20260721A.001-.010全部实现、自审与整链closeout完成；无产品行为、协议、schema、UI、视觉或测试coverage漂移。
* 范围：把当前可工作的shell-deck大文件按既有职责边界拆成更小模块；只改变源码组织，不改变逻辑、协议、UI、数据、时序或测试语义。
* 阶段：HTTP、Macro validation、App Room state、Macro session、Library session、Flow editor、runner、Terminal Room manager、workbench CSS和current test journeys均已完成拆分与Gate。

### Relations

* 前置：20260627A.039完成后的current-schema产品状态；current E2E采用目录级自动发现，冻结历史journey不进入current suite；AgentEvent exact wait-limit专项仍由20260627A.038冻结。
* 子任务：20260721A.001-.010。
* 保持：全部active specs、公开API、server-authoritative Room语义、single-controller/content lease、MacroDefinitionV5、AgentEvent exact wait limit、retained terminal view及既有UI与自动化行为。
* 排除：功能开发、schema/API演进、兼容层、视觉重做、性能策略调整和历史`.031B`基线修改。
