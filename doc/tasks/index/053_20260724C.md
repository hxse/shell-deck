# Task Index 20260724C

## Task ID `20260724C`

### Summary

* 路径：`doc/tasks/snapshots/20260724C macro editor input performance/20260724C/`
* 状态：Formal Document、Code、Test、Current Docs与Close Gate完成；未解决P1/P2为零。
* 范围：Macro Visual editor共享draft mutation性能、精确dirty基线、可编辑ID的稳定DOM identity、连续输入/删除与无隐式Save回归。
* 相关active spec / guide：完成后同步`macro_template_contract.md`；用户操作入口不变，不更新guide。
* 摘要：保留唯一deep rune draft和同步字段mutation，移除每次按键的完整definition clone/root replacement、重复base序列化及message JSON clone；persisted ID不再直接或间接兼任编辑器key，For(text-list)行以item object保持子输入DOM稳定。24项focused unit/structure、1项focused Chromium、完整295项unit/integration及58项单worker E2E通过。

### Relations

* `follow-up 20260723C.008`：继续收敛Macro record session的Visual draft mutation热路径，不改变其唯一rune state ownership。
* `follow-up 20260723C.012`：修正递归Flow presentation中editable node ID兼任DOM identity的问题。
