# Task Index 20260627A.032

## Task ID `20260627A.032`

### Summary

* 路径：doc/tasks/snapshots/20260627A shell deck bootstrap/20260627A.032 multi-room routing and user-level content storage/
* 状态：foundation-gate-passed（代码已落地；不是standalone release candidate）
* 范围：删除Project/configId/Directory scope与目标工作目录模型；建立一个server多Room、Room Home与32个live Room上限、MacroRecord/shared-store resource transaction底座、Room-scoped AgentEvent、per-terminal cwd、browser-local preference与统一generated ID；notification配置只迁移路径、内容不变。
* 相关 active spec / guide：本 task 02_spec/** 已按实现收口；architecture、Room terminal sync、User Data Root、Macro ownership、run evidence active specs、README 与 quickstart 已同步。
* 摘要：.032独立闭合Room/storage foundation，.033承接single-writer与共享内容编辑租约，MacroDefinition/API/editor/runner在.034原子cutover，不造中间schema；notification在target-scoped跨进程锁内从legacy path按raw bytes迁到User Data Root并切断dual read；最终Macro API无duplicate/clone。/在零Room时自动进入首个随机Room，已有Room时显示管理Home；Home集中New/Open/Destroy，Room页只打开Home，单process容量固定32且不做idle回收。

### Relations

* 父任务：20260627A。
* 修正：根任务及 .002/.003/.005/.013 把 configId 同时用作 terminal collaboration、storage、Prompt 和 layout scope；同时撤销尚未实现的 Project/canonical cwd 提案。
* 延续：.002 的 multi-tab terminal sync、.030 的 replay/backpressure，以及 .031 的 browser editor state ownership。
* 回归基线：继承 `.031B` 的不可变历史 inventory，并在本 workspace 以逐项归因的 current journey记录 `.032` 明确允许的中间态删除与新 Room/Home controls；surviving Room/terminal UI仍须全部通过真实 UI 操作。
* 前置于：.033的Room controller/content edit lease、.034的production Macro CRUD/MacroDefinitionV3/显式Prepare terminals/Start/run snapshot，以及.035的单一user-level Library；.032不是standalone release candidate。
* 排除：Macro terminal resolution、Library CRUD、PTY restoration、cross-process live Room federation、cloud sync、认证和通用旧数据migration；notification raw-byte path relocation是唯一明确例外。
