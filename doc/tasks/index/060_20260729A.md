# Task Index 20260729A

## Task ID `20260729A`

### Summary

* 路径：`doc/tasks/snapshots/20260729A flexible Parallel shared Text routing/20260729A/`
* 状态：已完成；Formal Document、Execution、Code、Focused Test、Current Docs与Overall Close Gate全部通过。
* 级别：三星任务。
* 范围：破坏性切换MacroDefinitionV6，把Parallel改为per-Action terminal的线性pane，并为shared Text提供pane-order/completion-order实时append queue。
* 相关active spec / guide：已同步`macro_template_contract.md`、`run_log_contract.md`、`shell_deck_architecture.md`、storage contract、README与Quickstart。
* 摘要：Shell由单一pane独占；Text自动推导exclusive/shared；shared Text只允许Send，并由每次Parallel invocation独立queue实时写入。删除lane terminal、final Output、merge与`merged_text`，不迁移V5。

### Relations

* `replaces 20260627A.018`：删除其固定final Output与`merged_text` fan-in，把Parallel收口为普通Action的并发容器。
* `refines 20260722A.001`：不再以Collect lane text开关隐藏Output，而是删除强制output authoring。
* `refines 20260723C.011`：重用pure policy/stateful controller边界，但按新V6 contract替换旧lane terminal/output commands。
* `follows 20260727A`：作为当前jj栈顶后继，不修改QR login与access contract。
