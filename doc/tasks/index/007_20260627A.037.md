# Task Index 20260627A.037

## Task ID `20260627A.037`

### Summary

* 路径：`doc/tasks/snapshots/20260627A shell deck bootstrap/20260627A.037 persistable unassigned terminal and artifact references/`
* 状态：implementation-complete，Document/Code/Test Gate通过。
* 范围：破坏性切换到`MacroDefinitionV4`，把terminal target与必填Action artifact source建模为exact tagged reference；`{kind:"unassigned"}`是可持久化但不可运行的schema分支。
* 验证：拆分persistable definition validation、runnable completeness validation与live Room validation。Macro/Library Save只要求persistable；Start同时要求runnable与Room ready。
* UI：`Unassigned`是各引用selector内的真实选项；凡slot允许该branch，新建时无条件默认Unassigned，即使已有compatible候选也必须由用户显式指派。选中时显示局部warning；不增加Macro级placeholder mode，也不把空文本、optional field或`none`解释为unassigned。

### Relations

* 父任务：20260627A。
* 硬前置：.034 Macro editor/Prepare/runner、.035 server-authoritative runtime sync、.036 Library text gateway与Load。
* 替换：`.034-.036`的`MacroDefinitionV3`、primitive `terminalIndex`引用、空`stepId` visual placeholder，以及artifact part缺省source运行成空字符串的隐式行为。
* 保持：Room/controller/content lease、terminal runtime identity、显式Prepare、Library storage与Copy/Load identity语义。
* 排除：通用表单placeholder、旧schema兼容、migration、automatic reference repair与runtime空值代填。
