# Task Index 20260724D

## Task ID `20260724D`

### Summary

* 路径：`doc/tasks/snapshots/20260724D full-stack hot path performance/20260724D/`
* 状态：Formal Document、Code、Test、Current Docs与Close Gate完成；完整Gate全绿。
* 范围：Macro live diagnostics/dirty、AgentEvent indexed wait、Text incremental hash sync、Room轻量structure projection、Runner definition-once delta、Trace两级pagination与局部O(N²)收敛。
* 相关active spec / guide：已同步Macro、AgentEvent、Room terminal、run log、architecture active specs与Quickstart。
* 审阅：`04_review/01_review_and_verification.md`记录十五项已修P2/P3、完整Gate与明确残余风险。
* 摘要：采用current-schema-only的`revision + incremental mutation + merged full-state SHA-256`，删除Text/Runner旧全量live路径；Trace按run summary与event page分层加载，不改变durable evidence schema或retention。

### Relations

* `follow-up 20260724C`：继续移除Visual Macro输入事件中的重复全树validation、clone和dirty stringify。
* `follow-up 20260723C.001/.002/.004/.005/.006`：在既有store/publication/Room coordinator拆分边界内优化，不重新集中ownership。
* `follow-up 20260627A.030/.031/.032/.035/.038`：替换Text、Room、Runner与AgentEvent初版全量或轮询热路径，保留其identity、revision、repair、Pause和durability不变量。
