# Task Index 20260723C.006

## Task ID `20260723C.006`

### Summary

* 路径：`doc/tasks/snapshots/20260723C bounded mechanical decomposition/20260723C.006 split Room registry lifecycle and public facade/`
* 状态：Formal Document、Code与Test Gate完成；未解决P1/P2为零。
* 级别：三星任务。
* 范围：把`terminalRoomManager.ts`的Room registry/create/list/lookup/destroy lifecycle移入专用coordinator，manager保留唯一public facade、map truth与cross-domain wiring。
* 相关active spec：`shell_deck_architecture.md`、`room_terminal_contract.md`。
* 摘要：在`.004/.005`稳定control/backend边界后，把485行manager收至389行，新增164行registry lifecycle coordinator；唯一Room/client maps仍由manager创建，冻结capacity、route generation、destroy admission/drain顺序。focused unit/integration与Home lifecycle E2E通过。

### Relations

* `child of 20260723C`。
* `follows 20260723C.005`。
* `refines 20260723B.001`与`20260721A.008`。
