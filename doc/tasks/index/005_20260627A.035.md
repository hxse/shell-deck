# Task Index 20260627A.035

## Task ID `20260627A.035`

### Summary

* 路径：doc/tasks/snapshots/20260627A shell deck bootstrap/20260627A.035 server-authoritative room runtime sync and unified mutation feedback/
* 状态：implementation-complete，Document/Code/Test Gate通过。
* 范围：把Macro runner、runtime input、Room内通知与saved-content invalidation统一为server-authoritative WebSocket push；删除runner polling和Home手动Refresh；为所有shared mutation拒绝建立统一toast；覆盖同URL双标签页接管与运行时同步。
* 核心边界：Macro/Library selection和未保存draft仍是browser-local；Save/Delete后的record是user-global；active run、冻结Macro、current node、Pause/Resume/Stop与runtime input是Room live state。
* 后继：`.036` Library复用本任务的saved-content invalidation、mutation feedback和single-writer呈现，不复制同步机制。

### Relations

* 父任务：20260627A。
* 前置：.032 Room lifecycle/WebSocket、.033 controller guard/content lease、.034 MacroDefinitionV3/version-bound run snapshot。
* 前置于：.036 user content Library。
* 排除：browser-to-browser同步、多人协作模型、runner持久恢复、Library业务UI、跨server-process live Room federation。
