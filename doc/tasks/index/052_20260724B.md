# Task Index 20260724B

## Task ID `20260724B`

### Summary

* 路径：`doc/tasks/snapshots/20260724B structured JSON submission capture/20260724B/`
* 状态：Formal Document、Code、Test、Current Docs与Close Gate完成；未解决P1/P2为零。
* 范围：增加Room/terminal/launch-bound的无参数`just submit-json`、canonical `SHELL_DECK_JUSTFILE`、root `structured-json` Capture、Schema-aware JSON Schema 2020-12校验、Schema-derived可查看/复制提示词、typed `captured_json` artifact、JSON Pointer `json_match`及统一canonical compact textual projection。
* 相关active spec / guide：`macro_template_contract.md`、`capture_agent_event_contract.md`、`run_log_contract.md`、`shell_deck_architecture.md`与Quickstart已同步。
* 摘要：不引入step ID、submission queue或新端口；当前Shell注入的runtime identity隔离Room，canonical justfile env避免checkout绝对路径固化，第一份schema-valid提交原子消费live waiter。local reference检查只遍历真正subschema，raw invalid draft逐字保留，HTTP固定token/body/path验证顺序。Visual editor从合法Schema生成建议提示词但不猜测Send，clipboard失败时保留可手动选择的正文。typed JSON保持单一真值；Send/Notify/Input default/Extract/`text_match`在共享read boundary取得稳定单行JSON，`json_match`继续typed。Pause/Stop/timeout、stable error、canonical JSON evidence与visual authoring均由focused及完整Gate覆盖。

### Relations

* `follows 20260724A`：承接MacroRecord成为唯一saved user content后的current产品状态。
* `refines 20260627A.006`：在既有Room-scoped ingest与terminal runtime context旁增加program-agnostic structured result入口，不把它伪装成Codex hook event。
* `extends 20260627A.038`：复用explicit unbounded/active-time timeout语义，同时保持AgentEvent Capture行为不变。
